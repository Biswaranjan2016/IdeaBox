<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.happy934.tempideabox">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>

    <uses-feature android:name="android.hardware.camera2.full" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".KeyBoardInput"
            android:windowSoftInputMode="adjustPan"/>
        <activity android:name=".ResultViewActivity" />
        <activity android:name=".temporaryCardTest.TemporaryCardTest" />
        <activity android:name=".resultInCardView.RecyclerViewResult" />
        <activity android:name=".database.DataBaseTransactions" />
        <activity android:name=".cameraFeature.Camera" />
        <activity android:name=".cameraFeature2.Camera" />
        <activity android:name=".camera.Cam" />
        <activity android:name=".camera.ImageAdapter$ShowDialog" />
        <activity android:name=".camera.ImageSelector" />
        <activity android:name=".speech.Mic"></activity>
    </application>

</manifest>